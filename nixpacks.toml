# Nixpacks — biarkan provider PHP pasang ekstensi dari composer.json (termasuk gd)
# https://nixpacks.com/docs/providers/php

[variables]
NIXPACKS_PHP_VERSION = "8.2"
NIXPACKS_PHP_ROOT_DIR = "/app/public"
